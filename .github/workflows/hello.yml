name: GitHub Actions Practice

on:
  push:

jobs:
  practice:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show repository files
        run: ls -la

      - name: Run a test
        run: test -f does-not-exist.txt
